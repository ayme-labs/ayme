// The Inspector's Tailwind stylesheet, compiled for its shadow root by the
// design system's shadow-tailwind plugin (see tsdown.config.ts).
declare module "virtual:ayme-inspector-css" {
  const css: string;
  export default css;
}
