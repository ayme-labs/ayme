{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build", "^typecheck"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test:e2e": {
      "dependsOn": ["build", "^build"],
      "passThroughEnv": ["PLAYWRIGHT_BROWSERS_PATH"],
      "cache": false
    },
    "test:goals": {
      "dependsOn": ["^build"],
      "passThroughEnv": ["PLAYWRIGHT_BROWSERS_PATH", "AYME_OPENROUTER_API_KEY"],
      "cache": false
    }
  },
  "boundaries": {
    "tags": {
      "tooling": {
        "dependencies": {
          "deny": ["core", "adapter", "app"]
        }
      },
      "core": {
        "dependencies": {
          "deny": ["adapter", "app"]
        }
      },
      "adapter": {
        "dependencies": {
          "deny": ["app"]
        }
      }
    }
  }
}
